# Versions figées : ce fichier est le verrou de build, pas un plancher.
# Toute montée de version passe par une PR Dependabot validée par la CI.
# Le plancher de compatibilité de la bibliothèque est dans pyproject.toml.
Pillow==12.3.0
PySide6==6.11.1
pyinstaller==6.21.0
